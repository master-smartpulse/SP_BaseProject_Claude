# Status da Implementação

**Objetivo:** Tabela simples com spec/feature e nível de completude (%). Atualize sempre que o status de uma spec mudar.

**IMPORTANTE:** Ao finalizar qualquer comando do workflow (`/specify`, `/specify-tech`, `/plan`, `/tasks`, `/implement`, `/review`, `/constitution`), atualize este arquivo e o `FEATURE_LIST.md`.

---

| Spec | Completude |
|------|------------|
| (nenhuma) | — |

---

**Última atualização:** (a definir)